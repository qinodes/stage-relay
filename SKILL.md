---
name: stage-relay
description: Use when a development task involves multiple stages, requires persistent progress tracking across sessions, or requires automated testing and user acceptance before stages can be considered complete.
---

---

# Development Tracking

## 目的

大型、多階段的開發任務必須使用文件持續紀錄開發進度。

追蹤文件的目的：

- 紀錄任務最終目標。
- 紀錄任務拆分後的各個開發階段。
- 紀錄每個階段目前的開發狀態。
- 紀錄自動測試結果。
- 紀錄使用者驗收狀態。
- 讓新的 Session 或 Agent 可以在缺少先前對話內容的情況下接續開發。

開發進度不得只存在於對話 Context 中。

對於已建立追蹤文件的任務，追蹤文件是開發進度的主要依據。

---

# 任務追蹤文件位置

所有大型、多階段開發任務統一存放於：

```text
docs/tasks/
```

每個任務建立獨立資料夾，使用三位數流水編號：

```text
docs/
└── tasks/
    ├── 001/
    │   └── task.md
    ├── 002/
    │   └── task.md
    ├── 003/
    │   └── task.md
    └── ...
```

每個任務的主要追蹤文件固定為：

```text
docs/tasks/<編號>/task.md
```

例如：

```text
docs/tasks/007/task.md
```

---

# 任務編號規則

建立新任務前，必須先檢查：

```text
docs/tasks/
```

找出目前存在的最大任務編號。

新任務使用：

```text
目前最大編號 + 1
```

例如目前存在：

```text
docs/tasks/001/
docs/tasks/002/
docs/tasks/003/
```

新任務必須建立：

```text
docs/tasks/004/
```

編號固定使用三位數格式：

```text
001
002
003
...
009
010
011
...
099
100
```

不得：

- 重複使用既有編號。
- 因為舊任務完成而重新使用編號。
- 因為任務被刪除而重新排列編號。
- 修改既有任務的編號。

如果：

```text
docs/tasks/
```

不存在，建立該目錄。

如果目前沒有任何任務，第一個任務從：

```text
001
```

開始。

---

# 建立新任務

當新的開發工作符合此 Skill 的使用條件時，必須建立新的任務追蹤文件。

建立流程：

1. 檢查 `docs/tasks/`。
2. 找出目前最大任務編號。
3. 取得下一個任務編號。
4. 建立 `docs/tasks/<編號>/`。
5. 建立 `task.md`。
6. 寫入任務目標。
7. 將任務拆分成多個可以獨立開發、測試與驗收的階段。
8. 為每個階段定義目標。
9. 為每個階段定義驗收條件。
10. 將 Overall 狀態設定為 `開發中`。
11. 開始第一個尚未完成的階段。

例如目前最大任務為：

```text
docs/tasks/012/
```

新的任務必須建立：

```text
docs/tasks/013/task.md
```

---

# 任務文件必要內容

每個：

```text
docs/tasks/<編號>/task.md
```

至少必須包含以下資訊：

1. 任務目標
2. Overall 狀態
3. 開發階段
4. 每個階段的目標
5. 每個階段的目前狀態
6. 每個階段的驗收條件
7. 自動測試結果
8. 必要的開發備註

---

# Overall 狀態

Overall 狀態只允許：

```text
開發中
完成
```

新建立的任務預設：

```text
Overall 狀態：開發中
```

只有所有階段皆為：

```text
結束
```

Overall 狀態才可以更新為：

```text
Overall 狀態：完成
```

不得在仍有未完成階段時將 Overall 狀態設定為 `完成`。

---

# 階段拆分

大型任務開始開發前，必須先拆分成多個階段。

每個階段應該：

- 有明確目標。
- 有明確範圍。
- 可以獨立進行開發。
- 可以執行相關自動測試。
- 有明確的使用者驗收條件。

避免建立過於模糊的階段，例如：

```text
階段一：開發功能
```

應該使用可以明確判斷是否完成的描述，例如：

```text
階段一：建立使用者登入 API

目標：
完成登入 API、Token 建立與錯誤處理。

驗收條件：
- 正確帳號密碼可以登入。
- 錯誤密碼會回傳錯誤。
- 登入成功會取得 Token。
```

---

# 階段狀態

每個階段只能使用以下狀態：

```text
開發中
開發完成
自動測試通過
使用者已驗收完成
結束
```

狀態必須依照以下順序轉移：

```text
開發中
↓
開發完成
↓
自動測試通過
↓
使用者已驗收完成
↓
結束
```

不得跳過任何狀態。

---

# 開發中

開始實作某個階段時，將該階段設定為：

```text
狀態：開發中
```

代表該階段目前正在進行程式碼修改或相關開發工作。

---

# 開發完成

當該階段要求的程式碼實作完成後，可以更新為：

```text
狀態：開發完成
```

`開發完成` 不代表該階段已經完成。

此狀態只代表：

- 程式碼實作完成。
- 尚未確認所有相關自動測試通過。

---

# 自動測試通過

進入：

```text
自動測試通過
```

之前，必須實際執行該階段相關的自動測試。

只有相關測試全部成功後，才可以更新：

```text
狀態：自動測試通過
```

並在 `task.md` 紀錄執行過的測試與結果。

例如：

```text
自動測試：
- Unit Tests：PASS
- Integration Tests：PASS
- Type Check：PASS
```

不得因為以下原因標記為 `自動測試通過`：

- 預期測試應該會成功。
- 程式碼看起來沒有問題。
- 沒有實際執行測試。
- 測試無法執行。
- 部分測試失敗。
- 尚未完成測試。

如果測試失敗：

1. 不得進入 `自動測試通過`。
2. 修正造成失敗的問題。
3. 再次執行相關測試。
4. 直到相關測試實際通過。

---

# 使用者驗收

自動測試通過後，不代表階段已經完成。

階段進入：

```text
狀態：自動測試通過
```

後，必須交由使用者驗收。

此時應告知使用者：

- 此階段完成了什麼。
- 自動測試結果。
- 使用者需要驗收哪些內容。

驗收內容應以 `task.md` 中該階段的「驗收條件」為主要依據。

Agent 不得自行假設使用者已經驗收。

只有使用者明確表示驗收通過後，才可以更新：

```text
狀態：使用者已驗收完成
```

如果使用者表示驗收失敗、發現 Bug 或要求修改：

1. 不得標記為 `使用者已驗收完成`。
2. 將階段恢復為 `開發中`。
3. 紀錄使用者回報的問題。
4. 修正問題。
5. 重新完成開發。
6. 重新執行相關自動測試。
7. 自動測試通過後再次交由使用者驗收。

---

# 階段結束

只有當階段已經：

```text
使用者已驗收完成
```

才可以更新為：

```text
狀態：結束
```

`結束` 代表該階段正式完成。

只有 `結束` 的階段才能被視為完整完成。

---

# 下一階段

目前階段完成並進入：

```text
結束
```

後，才能正式進入下一個尚未完成的階段。

開始下一階段時：

1. 更新下一階段狀態為 `開發中`。
2. 根據該階段目標進行開發。
3. 完成後執行自動測試。
4. 自動測試通過後等待使用者驗收。
5. 使用者驗收完成後結束該階段。

---

# 接續既有任務

如果使用者要求接續某個既有任務，例如：

```text
繼續 task 013
```

必須先讀取：

```text
docs/tasks/013/task.md
```

開始修改程式碼之前，先確認：

1. 任務目標。
2. Overall 狀態。
3. 任務有哪些階段。
4. 哪些階段已經結束。
5. 目前正在進行哪個階段。
6. 目前階段的狀態。
7. 目前階段的驗收條件。
8. 已執行的自動測試。
9. 開發備註。

然後從目前狀態接續開發。

不得因為新的 Session 沒有之前的對話 Context，而：

- 重新建立相同任務。
- 重新開始已完成階段。
- 忽略既有追蹤文件。
- 假設之前的工作沒有完成。

---

# 接續開發原則

`task.md` 是跨 Session 開發時的主要進度依據。

每次開始接續大型任務時，都應先讀取對應的 `task.md`。

每次階段狀態改變後，都必須同步更新 `task.md`。

例如：

```text
開發中
→ 開發完成
```

必須更新文件。

```text
開發完成
→ 自動測試通過
```

必須更新文件。

```text
自動測試通過
→ 使用者已驗收完成
```

必須更新文件。

```text
使用者已驗收完成
→ 結束
```

也必須更新文件。

不得只在對話中描述狀態變化而不更新追蹤文件。

---

# 狀態與實際情況不一致

如果發現：

- 對話 Context
- `task.md`
- 實際程式碼
- Git 狀態
- 自動測試結果

彼此不一致，不得直接猜測目前進度。

應先檢查：

1. `task.md`
2. 實際程式碼
3. Git diff / Git history（如果適用）
4. 自動測試結果

確認實際狀態後，再修正 `task.md`。

不得為了符合文件內容而假裝尚未完成的工作已完成。

---

# 任務追蹤文件範本

建立新的 `task.md` 時，使用以下結構：

```markdown
# 開發任務追蹤

## 任務目標

描述這次大型開發任務最終需要完成的目標。

---

## Overall 狀態

開發中

---

## 階段

### 階段一：<階段名稱>

#### 目標

描述此階段需要完成的內容。

#### 狀態

開發中

#### 驗收條件

- 條件一
- 條件二
- 條件三

#### 自動測試

尚未執行。

#### 備註

無。

---

### 階段二：<階段名稱>

#### 目標

描述此階段需要完成的內容。

#### 狀態

開發中

#### 驗收條件

- 條件一
- 條件二

#### 自動測試

尚未執行。

#### 備註

無。

---

### 階段三：<階段名稱>

#### 目標

描述此階段需要完成的內容。

#### 狀態

開發中

#### 驗收條件

- 條件一
- 條件二

#### 自動測試

尚未執行。

#### 備註

無。
```

---

# 自動測試紀錄範例

自動測試尚未執行：

```text
#### 自動測試

尚未執行。
```

測試成功：

```text
#### 自動測試

- Unit Tests：PASS
- Integration Tests：PASS
- Type Check：PASS
```

測試失敗：

```text
#### 自動測試

- Unit Tests：PASS
- Integration Tests：FAIL
- Type Check：PASS

失敗原因：
PaymentService integration test 發生 timeout。

目前正在修正。
```

不得在存在失敗測試時將階段標記為：

```text
自動測試通過
```

---

# 使用者驗收失敗範例

如果目前：

```text
狀態：自動測試通過
```

但使用者回報：

```text
登入後重新整理頁面會被登出
```

更新為：

```text
#### 狀態

開發中

#### 備註

使用者驗收失敗。

問題：
登入後重新整理頁面會被登出。

需要修正後重新執行自動測試。
```

修正完成後重新走：

```text
開發完成
↓
自動測試通過
↓
等待使用者驗收
```

不得沿用修改前的自動測試結果直接進入使用者驗收完成。

---

# 任務完成

只有所有階段都已經：

```text
狀態：結束
```

整個任務才可以完成。

確認所有階段結束後，將：

```text
Overall 狀態：開發中
```

更新為：

```text
Overall 狀態：完成
```

---

# 完成後保留任務

任務完成後不得刪除追蹤文件。

例如：

```text
docs/tasks/013/
└── task.md
```

即使：

```text
Overall 狀態：完成
```

仍然必須保留。

這些文件作為：

- 開發歷史。
- 任務設計紀錄。
- 測試紀錄。
- 使用者驗收紀錄。
- 後續問題追蹤參考。

後續建立新任務時，繼續使用下一個流水編號。

---

# 核心規則

任何符合此 Skill 使用條件的大型、多階段開發任務：

```text
建立 task
↓
定義任務目標
↓
拆分階段
↓
開發
↓
開發完成
↓
執行自動測試
↓
自動測試通過
↓
使用者驗收
↓
使用者驗收完成
↓
階段結束
↓
下一階段
```

所有階段結束後：

```text
Overall 狀態 → 完成
```

必須持續維護：

```text
docs/tasks/<編號>/task.md
```

使任何新的 Session 或 Agent 都能透過該文件理解目前進度並正確接續開發。
