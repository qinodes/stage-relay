---
name: stage-relay
description: 複数のSessionやAgentにまたがる進捗管理が必要な大規模・複数段階の開発タスクで使用し、段階ごとの受け入れ確認と保留後の一括確認の両方に対応し、各段階が自動テストとユーザー受け入れ確認を通過してから完了するようにします。
---

# 開発タスクリレー追跡

## 目的

大規模で複数段階にわたる開発タスクでは、次の内容をファイルに継続して記録しなければなりません。

- タスクの最終目標。
- 分割した各開発段階。
- 各段階の現在の開発ステータス。
- 自動テストの結果。
- ユーザー受け入れ確認の状態。
- 必要な開発メモ。

開発の進捗を会話のContextだけに残してはいけません。追跡ファイルを作成した後は、そのファイルを進捗の主要な情報源とし、過去の会話がない新しいSessionやAgentでも正しく作業を引き継げるようにします。

## 保存場所とタスク番号

`stage-reley/` は現在のプロジェクトルート内に作成しなければなりません。Git repositoryの場合はrepository rootを使用し、それ以外の場合はユーザーが指定したプロジェクトのルートを使用します。グローバルな場所、ユーザーのホームディレクトリ、Agentの設定ディレクトリ、またはインストール済みSkillのディレクトリには作成してはいけません。

このSkillを使用するたびに、プロジェクト内に次の構造が存在することを確認します。

```text
<プロジェクトルート>/
└── stage-reley/
    ├── .gitignore
    └── tasks/
        ├── 001/
        │   └── task.md
        ├── 002/
        │   └── task.md
        └── 003/
            └── task.md
```

`stage-reley/.gitignore` を自動的に作成します。内容は次の1行だけとし、ほかの内容を追加してはいけません。

```gitignore
*
```

すべての追跡ファイルをプロジェクト内の `stage-reley/tasks/` に保存します。タスクごとに3桁の連番を付けた専用ディレクトリを作成し、主要な追跡ファイルを `stage-reley/tasks/<番号>/task.md` とします。このSkill内にあるすべての `stage-reley/...` パスは、現在のプロジェクトルートを基準とします。

新しいタスクを作成する前に `stage-reley/tasks/` を確認し、既存タスクの最大番号を特定します。新しいタスクには「現在の最大番号 + 1」を使用します。ディレクトリが存在しない場合は作成し、タスクが1件もない場合は `001` から開始します。

たとえば、次のタスクが存在する場合：

```text
stage-reley/tasks/001/
stage-reley/tasks/002/
stage-reley/tasks/003/
```

次のタスクは以下の場所に作成します。

```text
stage-reley/tasks/004/task.md
```

番号は常に `001`、`009`、`010`、`099`、`100` のような3桁形式にします。次の操作は禁止します。

- 既存の番号を再利用する。
- 古いタスクが完了したことを理由に番号を再利用する。
- タスクが削除されたことを理由に番号を振り直す。
- 既存タスクの番号を変更する。

## タスクの作成と分割

新しい開発作業がこのSkillの適用条件に該当する場合、次を実行しなければなりません。

1. 現在のプロジェクトルートを特定し、その中に `stage-reley/.gitignore` と `stage-reley/tasks/` が存在することを確認する。
2. `stage-reley/tasks/` を確認し、次のタスク番号を取得する。
3. `stage-reley/tasks/<番号>/task.md` を作成する。
4. タスク目標を記録し、Overallステータスを `開発中` にする。
5. タスクを、個別に開発・テスト・受け入れ確認できる複数の段階へ分割する。
6. 各段階について、名前、目標、現在のステータス、受け入れ条件、自動テスト結果、必要な開発メモを記録する。
7. 最初の未完了段階を開始する。

たとえば、現在の最大タスク番号が `012` の場合、新しいタスクを `stage-reley/tasks/013/task.md` に作成します。

各段階には次の要素が必要です。

- 明確な目標と範囲。
- 個別に実施できる開発内容。
- 実行可能な関連自動テスト。
- 明確なユーザー受け入れ条件。

次のような曖昧な段階は避けます。

```text
段階1：機能を開発する
```

完了を明確に判断できる記述を使用します。

```text
段階1：ユーザーログインAPIを作成する

目標：
ログインAPI、Tokenの作成、エラー処理を完成させる。

受け入れ条件：
- 正しいアカウントとパスワードでログインできる。
- パスワードが間違っている場合はエラーを返す。
- ログインに成功するとTokenを取得できる。
```

## ステータスの流れ

### Overallステータス

Overallステータスには次の値だけを使用できます。

```text
開発中
完了
```

新しいタスクの初期値は `開発中` です。すべての段階が `終了` した場合に限り、Overallステータスを `完了` に変更できます。未完了の段階が1つでもある間は `完了` にしてはいけません。

### 段階ステータス

各段階では次のステータスだけをこの順番で使用し、途中のステータスを飛ばしてはいけません。

```text
開発中
↓
開発完了
↓
自動テスト合格
↓
ユーザー受け入れ完了
↓
終了
```

- `開発中`：コード変更または関連する開発作業を実施中です。
- `開発完了`：その段階で必要な実装は完了していますが、関連するすべての自動テストが合格したことはまだ確認されていません。そのため、この段階自体の完了を意味しません。
- `自動テスト合格`：関連テストを実際に実行してすべて合格し、実行したテストと結果を `task.md` に記録済みです。
- `ユーザー受け入れ完了`：自動テストの合格後、ユーザーが明示的に受け入れを承認済みです。Agentが承認済みと推測してはいけません。
- `終了`：`ユーザー受け入れ完了` の後にだけ設定でき、その段階が正式かつ完全に完了したことを表します。

段階のステータスが変わるたびに `task.md` を同期して更新します。会話内だけでステータス変更を報告してはいけません。

```text
開発中 → 開発完了
開発完了 → 自動テスト合格
自動テスト合格 → ユーザー受け入れ完了
ユーザー受け入れ完了 → 終了
```

次のいずれかを満たす場合に限り、次の未完了段階を開始できます。

- 現在の段階が `終了` している。
- 現在の段階が `自動テスト合格` で、ユーザーが受け入れ確認を保留して次の段階へ進むことを明示的に選択した。

受け入れ確認の保留によって現在の段階のステータスは変わりません。また、受け入れ承認や段階の終了を意味しません。ユーザーの決定を `task.md` のメモに記録します。ユーザーが明示的に保留を選択していない場合、次の段階を開始してはいけません。

## 自動テスト

`自動テスト合格` に進む前に、関連するテストを実際に実行しなければなりません。関連するすべてのテストが合格した場合に限り、ステータスを更新して結果を `task.md` に記録できます。

次の理由だけで `自動テスト合格` にしてはいけません。

- テストは合格するはずだと予想している。
- コードに問題がないように見える。
- テストを実際には実行していない。
- テストを実行できない。
- 一部のテストが失敗した。
- テストがまだ完了していない。

テストが失敗した場合は `自動テスト合格` に進めません。結果を記録し、問題を修正して、関連するすべてのテストが実際に合格するまで再実行します。

## ユーザー受け入れ確認

自動テストの合格だけでは段階は完了しません。段階が `自動テスト合格` になったら、ユーザーに次の内容を伝えます。

- その段階で完了した内容。
- 自動テストの結果。
- ユーザーが確認する必要のある内容。

確認内容は主に `task.md` にあるその段階の受け入れ条件に基づきます。その後、ユーザーの明示的な回答に応じて次の3つの結果のいずれかを処理します。

1. **受け入れ承認**
   - ステータスを順に `ユーザー受け入れ完了`、`終了` へ更新する。
   - 次の未完了段階を開始できる。
2. **受け入れ確認を保留**
   - ステータスを `自動テスト合格` のままにし、`ユーザー受け入れ完了` や `終了` にしない。
   - ユーザーが受け入れ確認を保留した決定を `task.md` に記録する。
   - 次の段階を先に開始できる。後で各段階を個別に確認することも、予定したすべての段階が自動テストに合格してから一括確認することもできる。
3. **問題の報告、受け入れ不合格、または変更要求**
   - `ユーザー受け入れ完了` にせず、新しい次の段階も開始しない。
   - 問題のある段階を `開発中` に戻し、ユーザーのフィードバックを `task.md` に記録する。
   - 問題を修正して開発を再度完了し、関連する自動テストを再実行する。変更前のテスト結果を流用してはいけない。
   - すべてのテストが合格した後、受け入れ承認、保留、追加フィードバックのいずれかをユーザーに再度確認する。

ユーザーが受け入れ確認の保留と、修正が必要な問題の両方を伝えた場合は、「問題の報告」として処理し、次の段階へ進んではいけません。Agentは曖昧な回答を受け入れ承認や保留の決定として解釈してはいけません。

一括確認では、`自動テスト合格` のまま残っているすべての段階について、完了内容、自動テスト結果、受け入れ条件をまとめてユーザーに提示します。ユーザーはすべての段階を一度に明示的に承認することも、一部だけを承認することもできます。

- 明示的に承認された各段階は、順に `ユーザー受け入れ完了`、`終了` へ更新する。
- 保留された段階、または明示的に承認されていない段階は `自動テスト合格` のままにする。
- 問題が報告された段階は `開発中` に戻して優先的に修正する。修正と再テストが完了するまで、新しい段階を開始してはいけない。

## 既存タスクの再開

ユーザーが「task 013を続けて」のように既存タスクの再開を求めた場合、コードを変更する前に `stage-reley/tasks/013/task.md` を読み、次を確認します。

1. タスクの目標。
2. Overallステータス。
3. タスクに含まれる段階。
4. すでに終了した段階。
5. 自動テストに合格したが、受け入れ確認が保留中または未実施の段階。
6. 現在進行中の段階。
7. 現在の段階のステータス。
8. 現在の段階の受け入れ条件。
9. 実行済みの自動テスト。
10. 開発メモ。

確認後、ファイルに記録された現在の状態から開発を再開します。新しいSessionに過去の会話Contextがないことを理由に、次のことをしてはいけません。

- 同じタスクを作り直す。
- 完了した段階をやり直す。
- 既存の追跡ファイルを無視する。
- 以前の作業が完了していないと推測する。

## 記録と実際の状態が一致しない場合

会話Context、`task.md`、実際のコード、Gitの状態、自動テスト結果が一致しない場合、現在の進捗を推測してはいけません。次の順に確認します。

1. `task.md`。
2. 実際のコード。
3. Git diff / Git history（該当する場合）。
4. 自動テスト結果。

実際の状態を確認してから `task.md` を修正します。ファイルの記述に合わせるために、未完了の作業を完了済みとして扱ってはいけません。

## タスク追跡ファイルのテンプレート

新しい `task.md` を作成するときは、次の構造を使用します。

```markdown
# 開発タスク追跡

## タスク目標

この大規模な開発タスクで最終的に達成する目標を記述する。

---

## Overallステータス

開発中

---

## 段階

### 段階1：<段階名>

#### 目標

この段階で完了する内容を記述する。

#### ステータス

開発中

#### 受け入れ条件

- 条件1
- 条件2
- 条件3

#### 自動テスト

未実行。

#### メモ

なし。

---

### 段階2：<段階名>

#### 目標

この段階で完了する内容を記述する。

#### ステータス

開発中

#### 受け入れ条件

- 条件1
- 条件2

#### 自動テスト

未実行。

#### メモ

なし。

---

### 段階3：<段階名>

#### 目標

この段階で完了する内容を記述する。

#### ステータス

開発中

#### 受け入れ条件

- 条件1
- 条件2

#### 自動テスト

未実行。

#### メモ

なし。
```

## 記録例

自動テストが未実行の場合：

```text
#### 自動テスト

未実行。
```

テストに合格した場合：

```text
#### 自動テスト

- Unit Tests：PASS
- Integration Tests：PASS
- Type Check：PASS
```

テストに失敗した場合：

```text
#### 自動テスト

- Unit Tests：PASS
- Integration Tests：FAIL
- Type Check：PASS

失敗理由：
PaymentServiceのintegration testがtimeoutした。

現在修正中。
```

失敗しているテストがある間は、その段階を `自動テスト合格` にしてはいけません。

ユーザーが受け入れ確認を保留した場合：

```text
#### ステータス

自動テスト合格

#### メモ

ユーザーはこの段階の受け入れ確認を保留し、次の段階へ進み、すべての段階が完了した後に一括確認することを選択した。
```

段階が `自動テスト合格` のときに、ユーザーから「ログイン後にページを再読み込みするとログアウトする」と報告された場合、次のように更新します。

```text
#### ステータス

開発中

#### メモ

ユーザー受け入れ確認に不合格。

問題：
ログイン後にページを再読み込みするとログアウトする。

問題を修正して自動テストを再実行する必要がある。
```

修正後は次の流れを繰り返します。

```text
開発完了
↓
自動テスト合格
↓
ユーザー受け入れ確認待ち
```

## タスク完了後の保持

すべての段階が `終了` した後、Overallステータスを `開発中` から `完了` に変更します。完了後も追跡ファイルを削除してはいけません。たとえば `stage-reley/tasks/013/task.md` を次の記録として保持します。

- 開発履歴。
- タスク設計の記録。
- テストの記録。
- ユーザー受け入れ確認の記録。
- 今後の問題追跡の参考資料。

以後のタスクには次の連番を使用し続けます。
